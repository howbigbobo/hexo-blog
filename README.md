# Instruction
```shell
hexo server
hexo new "post name"
hexo clean (first time)
hexo generate
hexo deploy
```

```shell
git commit --allow-empty -m "Trigger rebuild"
git push
```

tags: memo, linear-algebra